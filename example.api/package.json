{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon src/index.ts",
    "static": "yarn build && node dist/index.js",
    "build": "tsc",
    "docker-build": "docker build . -t example-api:$TAG && docker tag example-api:$TAG $REPO/example-api:$TAG && docker push $REPO/example-api:$TAG"
  },
  "dependencies": {
    "@types/node": "^18.11.9",
    "body-parser": "^1.20.0",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
